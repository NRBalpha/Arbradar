Only modify index.html. Never touch main.py.

Add a new tab called "Fast" between Tail-End 
and Market Browser tabs.

When Fast tab is clicked fetch /markets/fast?max_days=7
and display a table with columns:
MARKET | PLATFORM | CLOSES | PROBABILITY | ACTION

For CLOSES column show urgency badge:
TODAY = red badge
TOMORROW = orange badge
3 DAYS = amber badge
THIS WEEK = gray badge

If days_left < 2 show countdown:
"Closes in Xh Ym"

Each row clickable opens market popup.

At top of tab show toggle buttons:
[TODAY] [THIS WEEK] [ALL]
Clicking filters the table.

If no markets found show:
"No markets closing within 7 days found"
