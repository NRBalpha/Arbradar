Only modify index.html. Never touch main.py.

In updatePopupSplit() function, add this check
at the very beginning after getting the arb:

If arb.gap > 50, do not calculate profit at all.
Instead show this message in the split section:

"Gap is too large (X%) to be a real arb.
 Verify both markets are actually the same 
 event before placing any trade."

Show this in amber color and hide the YES/NO
split bar and all profit calculations entirely.

Only show split calculations when gap <= 50.
