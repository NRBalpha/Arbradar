You are the BACKEND agent for ArbRadar.
Only modify main.py. Never touch index.html.

Tasks:
1. Fix Metaculus — add METACULUS_API_TOKEN 
   support from .env file in the request headers
2. Remove ALL simulated/demo/fake data
3. Fix /arb/cross to deduplicate results
4. Make sure every platform fetch has retry logic


